<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="utf-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <meta name="description" content="{{ site.description }}">
    <meta name="author" content="{{ site.author }}">

    <link rel="shortcut icon" href="{{ site.url }}/bower_components/bootstrap/docs-assets/ico/favicon.png">

    <title>{{ title }} | {{ site.title }}</title>

    {% block css %}
    <link href="{{ site.url }}/assets/css/styles.min.css" rel="stylesheet">
    {% endblock %}

    <!-- HTML5 shim and Respond.js IE8 support of HTML5 elements and media queries -->
    <!--[if lt IE 9]>
    <script src="https://oss.maxcdn.com/libs/html5shiv/3.7.0/html5shiv.js"></script>
    <script src="https://oss.maxcdn.com/libs/respond.js/1.3.0/respond.min.js"></script>
    <![endif]-->

</head>

<body>
    <div class="container">
        <header class="row">
            <div class="col-md-2 logo">
                <img src="{{ site.url }}/assets/img/logo-white.png" width="128" height="128" alt="logo" title="logo" />
            </div>
            <div class="col-md-2 title">
                {{ site.title }}
            </div>
            <nav class="col-md-8">
                <ul class="nav nav-pills">
                    {% block navigation %}
                        <li>
                            <a href="{{ site.url }}">
                                Home
                            </a>
                        </li>
                        <li>
                            <a href="{{ site.url }}/photography">
                                Photography
                            </a>
                        </li>
                        <li class="clearfix">
                            <a href="{{ site.url }}/gear">
                                Gear
                            </a>
                        </li>
                        <li>
                            <a href="{{ site.url }}/route">
                                Route
                            </a>
                        </li>
                        <li>
                            <a href="{{ site.url }}/contact">
                                Contact
                            </a>
                        </li>
                    {% endblock %}
                </ul>
            </nav>
        </header>

        {% block main %}
            
        {% endblock %}

        <hr>

        <footer class="row">
            <p>&copy; {{ "now"|date("Y") }} {{ site.author }}</p>
        </footer>
    </div> <!-- /container -->


    <script src="{{ site.url }}/js/scripts.min.js"></script>
</body>
</html>
